spring:
  application:
    name: sausage-store

  cloud:
    vault:
      uri: http://${SPRING_CLOUD_VAULT_HOST}:8200
      token: ${SPRING_CLOUD_VAULT_TOKEN}
      kv:
        enabled: true
        backend: secret
        application: sausage-store