---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-service
  labels:
    app: {{ .Release.Name }}-{{ .Chart.Name }}-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.backend-report.containerPort.port }}
      protocol: TCP
      targetPort: {{ .Values.service.backend-report.containerPort.port }}
  selector:
    app: {{ .Release.Name }}-{{ .Chart.Name }}